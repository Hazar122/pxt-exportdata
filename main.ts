// Define a custom block
// @param text - the string to convert to JSON
// @param returns - the JSON object
// @category JSON
function stringToJson(text: string): any {
    return JSON.parse(text);
}


